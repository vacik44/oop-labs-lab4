app:
    data:
        repos:
            repository-path: calculations

    service:
        mapping:
            math-obj-map-file-path: build/resources/main/application.service.mapping.math-obj-map.yml
            math-solvers-map-file-path: build/resources/main/application.service.mapping.math-solvers-map.yml
